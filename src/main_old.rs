// use autogui::mouse::move_to;
// use std::time::Duration;

// fn main() {
//     loop {
//         // Move the mouse to a new random position
//         move_to(rand::random(), rand::random());
//         // Add a short delay to create a gliding effect
//         std::thread::sleep(Duration::from_millis(100));
//     }
// }